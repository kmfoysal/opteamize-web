import { FilterPipesPipe } from './filter-pipes.pipe';

describe('FilterPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
